// App.jsx (для тестового проекта — только один экран TestIdScreen)
import React from 'react';
import TestIdScreen from './screens/TestIdScreen.jsx';

export default function App() {
  return <TestIdScreen />;
}